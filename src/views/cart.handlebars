<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Carrito de Compras</title>
</head>
<body>
    <h1>Carrito de Compras</h1>
    {{#if cart}}
        <ul>
            {{#each cart.items}}
                <li>
                    {{this.product.name}} - Cantidad: {{this.quantity}} - Propietario: {{this.product.owner.username}}
                    <form action="/api/carts/remove" method="POST" style="display:inline;">
                        <input type="hidden" name="productId" value="{{this.product._id}}">
                        <button type="submit">Eliminar</button>
                    </form>
                </li>
            {{/each}}
        </ul>
        <form action="/api/carts/checkout" method="POST">
            <button type="submit">Checkout</button>
        </form>
    {{else}}
        <p>Tu carrito está vacío.</p>
    {{/if}}

    {{#if successMessage}}
        <p class="success">{{successMessage}}</p>
    {{/if}}

    {{#if errorMessage}}
        <p class="error">{{errorMessage}}</p>
    {{/if}}
</body>
</html>
